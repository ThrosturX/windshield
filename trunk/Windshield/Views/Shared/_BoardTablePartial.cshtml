@model Windshield.ViewModels.BoardTableViewModel

<!-- TODO: make it so the whole row is clickable -->

@if (Model.IsEmpty)
{
	<p class="lead">
		Sorry!
	</p>
	<p>
		There are no boards to be displayed for this game.
	</p>
}
else
{
<table class="table table-hover">

	<thead>
		<tr>
			<th>
				Game
			</th>
			<th>
				Owner
			</th>
			<th>
				Players
			</th>
			
		</tr>
	</thead>

	<tbody>
		@foreach (var row in Model.Rows)
		{
		<tr>
			<td>
				@Html.ActionLink(row.GameName, "JoinLobby", "Games", new {idBoard = @row.ID}, null)
			</td>
			<td>
				@row.OwnerName
			</td>
			<td>
				@row.Players / @row.MaxPlayers
			</td>
		</tr>
		}	
	</tbody>

</table>
}